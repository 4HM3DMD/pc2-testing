# Handover Document — March 6, 2026

> **Purpose:** Complete context for a new agent to pick up where this session left off.

---

## Agent Prompt (Copy This Into New Context Window)

```
You are resuming development on the ElastOS PC2 project — a decentralized Personal Cloud Computer built on Elastos.

## Essential Context

Read these files FIRST before doing anything:
1. `docs/core/SESSION_HANDOVER.md` — Current state, what's built, what's next
2. `docs/core/AGENT_HANDOVER.md` — Coding patterns, infrastructure, key files, gotchas
3. `docs/core/ROADMAP.md` — Strategic milestones mapped to DAO proposal
4. `.cursor/rules/` — All coding rules (MANDATORY — especially codequality.mdc)

## Current State

**Branch:** `feature/elacity-ddrm-marketplace`
**Base:** `main` at v1.1.0 (released 2026-03-03)

### What's Been Completed on This Branch (6 commits)

1. **Elacity dDRM Marketplace — Phase 1** (commit `6a55f430`)
   - Full Market dApp (browse, purchase, download media)
   - postMessage wallet bridge for iframe-sandboxed apps
   - COOP/COEP per-app headers for SharedArrayBuffer
   - App install/uninstall backend + APIs
   - `.edrm` file type support (icon, MIME, double-click opens player)

2. **Local IPFS Playback + Purchase Flow** (commit `1a225505`)
   - UnixFS DAG path resolution (`/ipfs/:cid/*` wildcard route)
   - Player streams from local Helia node, fallback to Elacity CDN
   - Particle Universal Account purchase flow fixed (SDK v1.0.24)
   - Lit Protocol pinned to v7.3.0 for DRM stability

3. **Elacity Player Source** (commit `7a8ca643`)
   - Player source at `pc2-node/data/test-apps/elacity-player-src/`

4. **Decentralized CDN Network** (commit `f78abdeb`)
   - NAT traversal: circuit-relay-v2, dcutr, autonat in Helia
   - Bitswap-first fetching: DHT findProviders → peer exchange before gateway fallback
   - CID announcement: purchased content announced on Kademlia DHT
   - Periodic re-announcement: all pinned CIDs re-announced every 4 hours
   - `pinned_cids` SQLite table (Migration 17)
   - In-memory CDN bandwidth tracking + `/api/cdn/stats` endpoint

5. **NAT Traversal Deps Fix** (commit `97574518`)
   - @libp2p/circuit-relay-v2, dcutr, autonat as direct dependencies

6. **Supernode IPFS Relay** (deployed, code at `deploy/ipfs-relay/`)
   - Standalone libp2p relay on 69.164.241.210:4003 (TCP) + 4004 (WS)
   - Peer ID: `12D3KooWMcuTWxkKg7xS3dxRaPDK9BEUHdAvKWf2b5Kdk4Kwxy9G`
   - Circuit relay + DHT server + AutoNAT + DCUtR
   - Running as systemd service `pc2-ipfs-relay`
   - Bootstrap addresses in `PC2_SUPERNODE_BOOTSTRAP` (ipfs.ts)

### Supernode (69.164.241.210)
- Boson DHT (39001/UDP), Active Proxy (8090/TCP), WireGuard (51820/UDP)
- Web Gateway (80/443), IPFS Relay (4003/TCP, 4004/WS)
- Elastos pg-oracle (updated to v0.0.3.3 on Mar 6)
- SSH: root@69.164.241.210 — password in password manager, NOT in git

### Unstaged Change
- `pc2-node/src/storage/ipfs.ts` — adds supernode bootstrap multiaddrs (public IPs only, no secrets)

## What's Next (Priority Order)

1. **App Center UI rebuild** — real backend APIs instead of hardcoded apps
2. **Auto-download on purchase** — auto-pin after buy, eliminate manual "Save to Cloud" step
3. **App registry manifest** — `app.json` schema for decentralized app distribution
4. **CDN dashboard UI** — show CDN stats in settings or status bar
5. **App Factory** — local build → IPFS pin → publish pipeline
6. **Creator tools** — media-packager integration
7. Continue through ROADMAP.md milestones

## Critical Rules
- NEVER run `npm start` from repo root — always `cd pc2-node && npm start`
- NEVER call React hooks inside JSX
- NEVER commit passwords, SSH credentials, or private keys
- All tasks must go through `.cursor/tasks/` workflow
- Check `.cursor/rules/codequality.mdc` before writing any React code
- Use `getBaseUrl(req, bosonService)` for all URL construction
- The Elacity Player MUST run in a popup window (SharedArrayBuffer requirement)
- `@lit-protocol/*` packages MUST stay pinned at v7.3.0
```

---

## Summary of All Work Done This Session

### Session Scope
This session covered the entire Elacity dDRM lifecycle: marketplace UI → purchase flow → DRM playback → local IPFS serving → decentralized CDN network → supernode relay deployment → Elastos node maintenance.

### Files Modified (Branch Commits)

| Area | Key Files | What Changed |
|------|-----------|-------------|
| Market dApp | `pc2-node/data/test-apps/elacity-market/*` | Full marketplace UI, GraphQL API, wallet integration |
| Player | `pc2-node/data/test-apps/elacity-player-src/*` | Gateway resolution, Lit SDK pinning |
| IPFS | `pc2-node/src/storage/ipfs.ts` | NAT traversal, Bitswap, DHT announce, supernode bootstrap |
| Gateway | `pc2-node/src/api/public.ts` | DAG path routing, CDN bandwidth tracking |
| Database | `pc2-node/src/storage/database.ts` | `pinned_cids` tracking, announcable CIDs query |
| Migrations | `pc2-node/src/storage/migrations.ts` | Migration 17: `pinned_cids` table |
| Schema | `pc2-node/src/storage/schema.sql` | `pinned_cids` table for fresh installs |
| Main server | `pc2-node/src/index.ts` | Periodic DHT re-announcement loop |
| Dependencies | `pc2-node/package.json` | NAT traversal packages as direct deps |
| GUI | `src/gui/src/helpers/open_item.js` | `.edrm` handler launches player popup |
| Relay | `deploy/ipfs-relay/*` | Standalone IPFS relay for supernode |

### Infrastructure Changes

| Change | Location | Status |
|--------|----------|--------|
| IPFS Relay service | 69.164.241.210:4003/4004 | Running (systemd) |
| pg-oracle update | 69.164.241.210 | Updated to v0.0.3.3 |
| Firewall rules | ufw 4003/tcp, 4004/tcp | Applied |

### Verified Working

- Purchase content via Elacity Market → wallet signature → on-chain buyAccess
- Download to local IPFS node → `.edrm` descriptor saved
- Play `.edrm` from file explorer → player popup → DRM license → local IPFS streaming
- CDN bandwidth tracking via `/api/cdn/stats`
- Supernode IPFS relay stable (500+ peers, ~88MB RAM)
- New PC2 nodes get NAT traversal packages automatically via `npm install`

### Known Issues / Deferred

- Player `pc2-wallet-provider.js` 404 — expected in popup mode (wallet bridge only works in iframe)
- `MetaMask - RPC Error: "pc2_getSmartAccountAddress" not found` — expected in popup (no bridge)
- Transaction confirmation UI shows "waiting" even after success — cosmetic issue for buyer
- Player loads from OPFS cache if content was previously played — user must clear OPFS to force local IPFS fetch

---

## Security Notes

- No passwords or SSH credentials are stored in any tracked file
- `data/identity.json` (contains private key) is gitignored
- `deploy/ipfs-relay/` code is clean — only public IPs (infrastructure addresses, not secrets)
- The supernode IP `69.164.241.210` appears in many files as public infrastructure config — this is expected and not sensitive
